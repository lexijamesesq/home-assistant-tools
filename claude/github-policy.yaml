# home-assistant-tools — public Claude Code tooling repo for HA work.
# Skills: /ha-update, /theme-update, /visual-diff. Config externalized to CLAUDE.md.
#
# History scan (2026-05-09): no credential or sensitive pattern in any commit.
# No baseline needed.
visibility: public
main_branch: main
main_branch_protection: direct_push
prep:
  required: true
  strictness: strict
  ttl_hours: 24
allowed_force_push_targets: []
protected_branches: [main]
secret_scanning_baseline: null
treat_as_public_for_secrets: false
